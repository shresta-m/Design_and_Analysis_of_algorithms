#include <bits/stdc++.h>
#include"arraysplit.h" 
using namespace std; 
int main() 
{ 
	int arr[] = {25, 45, -3, 12, 0,90,-10, 4, 28, -1, 4}; 
	int n = sizeof(arr)/sizeof(arr[0]); 
	arraysplit(arr, n); 
	return 0; 
} 